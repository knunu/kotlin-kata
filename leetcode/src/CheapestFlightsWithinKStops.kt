class CheapestFlightsWithinKStops {
    fun findCheapestPrice(n: Int, flights: Array<IntArray>, src: Int, dst: Int, K: Int): Int {
        return 0
    }
}

fun main() {
    val cheapestPrice: Int = CheapestFlightsWithinKStops().findCheapestPrice()
    println(cheapestPrice)
}